services:
  - type: web
    name: amnezia-vpn
    env: docker
    region: oregon
    plan: free
    healthCheckPath: /health
    dockerCommand: tail -f /dev/null   # keeps container alive
    envVars:
      SERVER_IP: "0.0.0.0"
      WG_PORT: "51820"
